import Header from '../components/Header'; import Footer from '../components/Footer'; import { useCart } from '../context/CartContext'; import Link from 'next/link'; export default function Cart(){ const { cart, removeFromCart, clearCart } = useCart(); const total = (cart||[]).reduce((s,i)=> s + (i.price||0)*i.qty, 0); return (<div><Header /><main className="container px-6 py-8"><h1 className="text-2xl font-bold mb-4">Cart</h1>{(!cart || cart.length===0) ? <p>Your cart is empty.</p> : (<div className="space-y-4">{cart.map(item=>(<div key={item.id} className="flex items-center gap-4 border p-3 rounded"><img src={item.image} className="w-24 h-24 object-cover" /><div className="flex-1"><h3 className="font-semibold">{item.name}</h3><p>PKR {item.price}</p><p>Qty: {item.qty}</p></div><button onClick={()=> removeFromCart(item.id)} className="text-red-600">Remove</button></div>))}<div className="border p-4 rounded"><p className="font-bold">Subtotal: PKR {total}</p><a href="/checkout" className="ml-4 px-4 py-2 bg-black text-white rounded">Checkout</a></div></div>)}</main><Footer /></div>)} 
