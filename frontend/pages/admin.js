import Header from '../components/Header'; import { useState } from 'react'; import API from '../lib/api'; export default function Admin(){ const [email,setEmail]=useState(''); const [pass,setPass]=useState(''); const login = async ()=>{ try{ const r = await API.post('/auth/login',{ email, password: pass }); localStorage.setItem('token', r.data.token); alert('Logged in'); }catch(e){ alert('Login failed') } }; return (<div><Header /><main className="container px-6 py-8"><h1 className="text-2xl font-bold mb-4">Admin Login</h1><input value={email} onChange={e=>setEmail(e.target.value)} placeholder="Email" className="w-full border p-2 mb-2" /><input value={pass} onChange={e=>setPass(e.target.value)} placeholder="Password" type="password" className="w-full border p-2 mb-2" /><button onClick={login} className="px-4 py-2 bg-black text-white rounded">Login</button></main></div>)} 
